<Page
    x:Class="QRCodeReader.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QRCodeReader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <CaptureElement Name="PreviewControl" Stretch="Uniform" Grid.Column="0"/>

        <StackPanel Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <Button Name="GetPreviewFrameButton" Content="GetPreviewFrameAsync" Margin="10" IsEnabled="False"/>
                <TextBlock Name="FrameInfoTextBlock" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBox x:Name="ResultText" IsReadOnly="True"/>
        </StackPanel>
    </Grid>
</Page>
